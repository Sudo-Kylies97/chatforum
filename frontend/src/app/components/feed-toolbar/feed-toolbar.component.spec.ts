import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FeedToolbarComponent } from './feed-toolbar.component';
describe('FeedToolbarComponent',()=>{let fixture:ComponentFixture<FeedToolbarComponent>;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[FeedToolbarComponent]}).compileComponents();fixture=TestBed.createComponent(FeedToolbarComponent);fixture.componentRef.setInput('categories',[{slug:'news',name:'News'}]);fixture.detectChanges();});it('renders supplied categories',()=>expect(fixture.nativeElement.textContent).toContain('News'));it('emits a search query',()=>{let value='';fixture.componentInstance.search.subscribe(v=>value=v);fixture.componentInstance.query='energy';fixture.componentInstance.search.emit(fixture.componentInstance.query);expect(value).toBe('energy');});});
