import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PostCardComponent } from './post-card.component';
import { Post } from '../../api.service';
const post:Post={id:1,author:{id:1,username:'alex',is_moderator:false},title:'Welcome',body:'Hello',category:null,is_misleading:false,created_at:'2026-01-01T00:00:00Z',comments:[],like_count:2,comment_count:0,liked_by_me:false,ai_status:'failed',embedding_status:'failed',ai_moderation_score:null,ai_moderation_rationale:null,ai_needs_review:null,vibe:'unknown',vibe_status:'disabled'};
describe('PostCardComponent',()=>{let fixture:ComponentFixture<PostCardComponent>;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[PostCardComponent]}).compileComponents();fixture=TestBed.createComponent(PostCardComponent);fixture.componentRef.setInput('post',post);fixture.detectChanges();});it('renders post content and like count',()=>{expect(fixture.nativeElement.textContent).toContain('Welcome');expect(fixture.nativeElement.textContent).toContain('2');});it('toggles collapsible comments',()=>{const button=fixture.nativeElement.querySelectorAll('.actions button')[1];button.click();fixture.detectChanges();expect(fixture.nativeElement.querySelector('.comments')).not.toBeNull();});});
